version: "3.9"
services:
  gorest:
    environment:
      - JWTSecretKey=12345678
      # - GOPATH=/home/root/project
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./:/home/root/project
    stdin_open: true
    tty: true
    command: "air -c src/.air_linux.toml"
