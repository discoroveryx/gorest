version: "3.9"
services:
  gorest:
    environment:
      - JWTSecretKey=12345678
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./src:/home/root/project
    stdin_open: true
    tty: true
    command: "air -c .air_linux.toml"
